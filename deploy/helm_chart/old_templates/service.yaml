apiVersion: v1
kind: Service
metadata:
  name: {{ include "csi-rclone.fullname" . }}
  labels:
    {{- include "csi-rclone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csi-rclone.selectorLabels" . | nindent 4 }}
