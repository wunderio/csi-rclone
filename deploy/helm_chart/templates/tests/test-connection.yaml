apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csi-rclone.fullname" . }}-test-connection"
  labels:
    {{- include "csi-rclone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csi-rclone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
